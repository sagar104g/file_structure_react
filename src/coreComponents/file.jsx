import React from 'react';

function folder(fileName) {
  return (
    <div>
        <img src="../../public/file.png" alt="file"></img>
        <p>{fileName}</p>
    </div>
  );
}

export default folder;
