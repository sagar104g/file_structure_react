import React from 'react';

function folder(folderName) {
  return (
    <div>
        <img src="../../public/folder.png" alt="folder"></img>
        <p>{folderName}</p>
    </div>
  );
}

export default folder;
